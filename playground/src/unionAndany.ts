let subs : number | string = "hello world"

let apireq : "pending" | "processing" | "success" // will accept only "pending" | "processing" | "success"

let ele :any // accept any tyep of data
// unknown safer than any
ele="abc"
ele=20
ele = 20.1

let ele2 :unknown

